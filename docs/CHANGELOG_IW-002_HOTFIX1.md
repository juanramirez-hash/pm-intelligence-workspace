# IW-002 HOTFIX 1

## Fixed

- Removed the obsolete `status` property from the `BusinessProduct` test fixture.
- Added `normalizedInventory` to the Executive Workspace state contract so the inventory domain reaches `buildWorkspaceContext`.

No production inventory calculations were changed.
