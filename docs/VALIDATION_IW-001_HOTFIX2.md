# Validation — IW-001 HOTFIX 2

Run:

```text
npm run build
npm run lint
npm test
```

The hotfix only updates the Executive Workspace input type so it can pass inventory metadata through the shared Workspace Context.
